#!/usr/bin/env python3

class Dog:
  def __init__(self, name, breed="Mutt"):
        self.name = name  # Save the name to self.name
        self.breed = breed  # Save the breed to self.breed (default is "Mutt")